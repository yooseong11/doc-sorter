# AGENTS.md

## 대화를 시작하면 무조건 먼저 읽기

1. [docs/STORY_MAP.md](docs/STORY_MAP.md) — 사용자와 스토리 라인
2. [docs/adr/README.md](docs/adr/README.md) — ADR 목록과 작성 규칙
3. `docs/adr/` 안의 모든 ADR 파일 (`0000-template.md` 제외)

읽은 뒤 요청과 관련된 스토리와 ADR 번호를 먼저 짧게 밝히고 작업을 시작합니다.

## 작업 규칙

- **ADR과 충돌하는 구현을 하지 않습니다.** 요청이 채택된 ADR과 부딪히면 작업 전에 어느 ADR과 충돌하는지 알립니다.
- **결정을 바꿔야 하면** 기존 ADR을 고치지 않고 새 ADR을 씁니다. 옛 ADR의 상태는 `대체됨(→ NNNN)`으로 바꿉니다. ([docs/adr/README.md](docs/adr/README.md) 규칙)
- **ADR에서 `보류`인 항목은 요청이 없으면 만들지 않습니다.**
- 스토리 맵에 없는 기능을 만들게 되면 스토리 맵의 어느 활동에 들어가는지 먼저 확인합니다.

## Git 규칙

- 혼자 쓰는 저장소입니다. **브랜치를 만들지 않고 `main`에 바로 커밋합니다.**
- **push는 사용자가 직접 합니다.** 에이전트는 push하지 않습니다.
- 커밋은 사용자가 요청할 때만 합니다.

## 커밋 메시지 규칙

- 형식: `타입: 한국어 요약` (예: `docs: 스토리 맵·ADR 정리와 AGENTS.md 추가`)
- 타입
  - `feat` 기능 추가
  - `fix` 버그 수정
  - `docs` 문서(스토리 맵, ADR, README 등)
  - `test` 테스트 추가·수정
  - `refactor` 동작은 그대로, 코드 구조만 변경
  - `style` CSS·화면 디자인 변경
  - `chore` 설정, 의존성, 코드 포맷(들여쓰기 등), 기타
- `feat`와 `style` 구분: 화면 요소를 추가해도 **로직(상태 변경, 데이터 처리 등)이 들어가면 `feat`**, 요소나 CSS만 추가·변경하고 동작이 없으면 `style`입니다.
  - 예: 드롭다운을 넣고 선택 시 카테고리가 바뀌면 `feat` / 드롭다운 모양만 넣었으면 `style`
- 요약은 한 줄로, 무엇을 했는지 적습니다.
- 필요하면 빈 줄 뒤 본문에 `- ` 목록으로 변경 내용을 적습니다.
- 결정(ADR)과 관련된 커밋이면 본문에 ADR 번호를 적습니다. (예: `ADR 0004`)

## 명령어

- `npm run dev` 개발 서버
- `npm run build` 빌드
- `npm run lint` oxlint
- `npm test` 테스트 (node --test)
