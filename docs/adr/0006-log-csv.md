# 0006. 분류 기록표 CSV

## 결정
`분류기록표_YYMMDD.csv`에 UTF-8 BOM을 넣고, 같은 날은 이어 쓴다.

## 이유
원래 파일명과 저장 경로의 대응표라 "그 파일 어디 갔지"를 찾을 수 있고, 회사 폴더 안에만 남아 0008을 지킨다. 엑셀은 BOM이 없으면 한글이 깨진다.

## 버린 대안
- 엑셀 xlsx: 라이브러리가 필요하고 이어 쓸 때 파일 전체를 다시 만들어야 한다.
- 파일명에 처리 날짜 넣기: 날짜가 두 개가 되어 헷갈린다(0005).
