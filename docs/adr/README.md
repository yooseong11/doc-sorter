# ADR (Architecture Decision Record)

결정 하나 = 파일 하나. 결정을 바꿀 때는 기존 파일을 고치지 않고 새 ADR을 쓰고, 옛 ADR의 상태를 `대체됨(→ NNNN)`으로 바꾼다.

파일명은 `NNNN-영문-슬러그.md`. 목록은 이 폴더를 그대로 본다 — 여기에 표로 옮겨 적지 않는다.

새 ADR은 [0000-template.md](0000-template.md)를 복사해서 쓴다.
