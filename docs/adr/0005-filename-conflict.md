# 0005. 파일명 유지와 충돌 처리

## 결정
원래 파일명을 유지하고, 같은 이름이 있으면 `_2`, `_3` …을 붙인다.

## 이유
파일명에 처리 날짜를 붙이면 원래 있던 날짜와 겹쳐 헷갈린다. 처리 날짜는 기록표에 남긴다(0006). getFileHandle은 덮어쓰므로 저장 전에 존재를 확인한다.

## 버린 대안
- AI가 파일명 새로 짓기: 보류. 추출 작업이라 R1 범위가 두 배가 되고, 틀린 이름은 조용히 틀려서 나중에 찾을 수 없다.
