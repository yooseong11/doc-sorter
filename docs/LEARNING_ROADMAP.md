# 학습 로드맵 — doc-sorter

> **현재 칸: 1-1**

## 이 파일 쓰는 법

**한 번에 한 칸만 본다.** 아래 칸은 앞 칸이 끝나기 전엔 읽지 않는다.

각 칸에는 세 가지가 있다.

- **열 것** — 어떤 파일을 여는가
- **할 것** — 10분 단위 행동
- **끝** — 여기까지 하면 끝. 더 안 해도 된다.

---

## 0단계 — 도구 켜기 ✅ 완료 (2026-09-22)

- [x] `tests/documentsReducer.test.js` 만들고 초록불 보기
- [x] `reset`을 일부러 망가뜨려서 빨간불 보기

배운 것: 테스트는 "지금 값이 맞나" 확인이 아니라 **"이 결정을 바꾸려면 나한테 먼저 걸려라"**는 장치다.

---

## 1단계 — 데이터의 수명

> 한 문장: **이 값이 만들어진 시점의 전제가 지금도 참인가?**
> 해당 결함: 1번(카테고리 정합성) · 2번(idb 레이스) · 6번(파생 state)

### 1-1. 버그를 말로 옮기기

- **열 것:** 아무것도. 메모장이나 종이.
- **할 것:** 10분. 1번 버그를 세 줄로 쓴다.
  - 무엇을 넣으면
  - 무엇이 나와야 하는데
  - 실제로 무엇이 나오나
- **끝:** 세 줄 썼으면 끝.

### 1-2. 실패하는 테스트 쓰기

- **열 것:** `tests/documentsReducer.test.js`
- **할 것:** 1-1의 세 줄을 `test()` 안으로 옮긴다. `documentsReducer`에 `category` 액션을 보내되, `options`에 없는 카테고리를 준다.
- **끝:** **빨간불이 뜨면 끝.** 고치지 않는다. 빨간불이 오늘의 성과다.

### 1-3. 초록불 만들기

- **열 것:** `src/lib/state/documentsReducer.js`
- **할 것:** 지워진 카테고리에 매달린 파일을 어떻게 할지 정하고 고친다.
  (미분류로 되돌리기 / 분류 후엔 카테고리 편집을 막기 — 둘 중 하나)
- **끝:** 초록불 + `docs/adr/0022-*.md` 한 장.

### 1-4. 같은 원리, 더 작은 것

- **열 것:** `src/components/ResultRow.jsx` 17번 줄
- **할 것:** `useState(needsReview)`가 왜 갱신되지 않는지 먼저 말로 설명해본다. 그 다음 고친다.
- **끝:** 재시도로 분류에 성공했을 때 펼침 상태가 따라오면 끝.

**졸업 시험:** `useState(props)`가 왜 위험한지 1분 안에 남한테 설명할 수 있으면 통과.

---

## 2단계 — 실패를 드러내는 법

> 한 문장: **잘못됐을 때 누가 어떻게 알게 되는가?**
> 해당 결함: 5번(에러 삼키기) · 7번(조용한 리듀서)

### 2-1. 세기만 하기

- **열 것:** 터미널
- **할 것:** `grep -rn "catch" src/ shared/ server/ api/` 를 치고, 그중 **아무것도 하지 않는 catch**가 몇 개인지 센다.
- **끝:** 숫자 하나 나오면 끝. 고치지 않는다.

### 2-2. 두 곳만 고치기

- **열 것:** `src/lib/save/saveFolder.js` 의 `pickRootDirectory`
- **할 것:** 사용자 취소(`AbortError`)와 진짜 실패를 구분한다. 진짜 실패면 `console.error`를 남긴다.
- **끝:** 취소했을 때와 진짜 에러일 때 콘솔이 다르면 끝.

### 2-3. 조용한 거부 드러내기

- **열 것:** `src/lib/state/documentsReducer.js` 49번 줄
- **할 것:** 검증에 걸려 무시된 경우를 사용자가 알 수 있게 만든다.
- **끝:** 드롭다운이 안 먹었을 때 화면에 무언가 뜨면 끝.

**졸업 시험:** 배포된 앱에서 버그 신고가 들어왔을 때, 로그로 원인을 좁힐 수 있는 자리가 어디인지 짚을 수 있으면 통과.

---

## 3단계 — 내 코드 밖의 세계

> 한 문장: **브라우저도 서버도 청구서도 내 사정을 봐주지 않는다.**
> 해당 결함: 3번(타임아웃 불일치) · 4번(공개 엔드포인트)

### 3-1. 읽기만

- **할 것:** Vercel 함수 실행 시간 제한 문서를 10분 읽는다.
- **끝:** Hobby 플랜 기본 제한이 몇 초인지 말할 수 있으면 끝.

### 3-2. 숫자 맞추기

- **열 것:** `api/classify.js` · `server/classifier.js` · `src/lib/classify/classify.js`
- **할 것:** 세 군데 타임아웃을 종이에 적고, 바깥이 안쪽보다 길도록 다시 정한다.
- **끝:** 세 숫자가 순서대로 정렬되면 끝.

### 3-3. 순차를 동시로

- **열 것:** `src/lib/state/runDocuments.js` 의 `classifyDocuments`
- **할 것:** for 루프를 3개씩 묶어 돌린다.
- **끝:** 파일 6개를 넣었을 때 눈에 띄게 빨라지면 끝.

### 3-4. 문 잠그기

- **열 것:** `api/classify.js`
- **할 것:** 요청 크기 상한 + IP당 호출 횟수 제한.
- **끝:** curl로 연달아 20번 때렸을 때 막히면 끝.

**졸업 시험:** 이 앱을 공개 URL에 올렸을 때 돈이 새는 경로를 말할 수 있으면 통과.

---

## 규칙

1. **한 칸씩.** 앞 칸이 안 끝났으면 다음 칸을 보지 않는다.
2. **끝 조건이 채워지면 끝.** "더 할 수 있는데"는 다음 칸으로 넘긴다.
3. **30분 막히면 물어본다.** 0분에 묻는 것과 30분 막히고 묻는 것은 남는 게 다르다.
4. **빨간불도 성과다.** 고치는 것보다 재현하는 게 어렵다.
5. 칸을 끝내면 맨 위 **현재 칸**을 고친다.
